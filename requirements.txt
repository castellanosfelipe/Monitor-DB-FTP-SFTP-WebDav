# Runtime dependencies — exact pins (Modo A se empaqueta offline con PyInstaller).
# La Fase 1 usa: paramiko, httpx, cryptography. El resto queda fijado desde ya
# para que el lockfile no cambie entre fases.

fastapi==0.115.8
uvicorn==0.34.0
APScheduler==3.11.0
jinja2==3.1.6

# Protocolos de archivos
paramiko==3.5.1
httpx==0.28.1

# Drivers de bases de datos (thin / Python puro)
pg8000==1.31.2
PyMySQL==1.1.1
python-tds==1.16.0
oracledb==2.5.1

# Secretos
cryptography==44.0.2

# Solo Windows (Modo A)
pywin32==308; sys_platform == "win32"
winotify==1.1.0; sys_platform == "win32"
pystray==0.19.5; sys_platform == "win32"
Pillow==11.1.0; sys_platform == "win32"
